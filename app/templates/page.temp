<section class="container-fluid clearfix" ng-repeat="section in content">
    <div ng-repeat="item in section.items" ng-class="item.class || ''">
        <div attr-name="{{item.type}}" content="item.content"></div>
    </div>
</section>